{
  "name": "ai-chatbot-cloudflare",
  "version": "1.0.0",
  "description": "AI-powered chatbot with persistent memory on Cloudflare Workers",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}